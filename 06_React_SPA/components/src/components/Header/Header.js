import React from 'react'
import logo from '../../logo.svg'

import './Header.css'

const Header = props =>{
    return(
        <header className={'Header'}>
            
            <a href="#">Home Page</a>  
        </header>
    )
}

export default Header;